# 发布政策

本政策区分 Git 源码发布与 GitHub Release 二进制发布。Apache License 2.0 仅适用于灵映自研源码，不会替代模型、数据集、字体、虚拟形象或厂商运行库各自的许可。

## Git 源码仓库

Git 历史及自动生成的源码归档不得包含：

- APK、AAB、APKS 或其他安装包；
- 模型权重、训练数据、测试数据和评测输出；
- Android 签名密钥、证书私钥、密码或本机配置；
- Qualcomm QAIRT/QNN/HTP SDK 头文件、工具和专有运行库；
- QNN 工具链生成的模型库及其他未经确认可再分发的二进制。

## GitHub Releases

项目维护者可以将签名 APK 作为 GitHub Release 附件。每个 Release 必须同时提供：

1. 版本号、发布日期和主要变更；
2. APK 的 SHA-256；
3. Android 版本、ABI、已验证设备和推理后端范围；
4. `THIRD_PARTY_NOTICES.md` 或等价的第三方声明链接；
5. 模型、虚拟形象和厂商运行库的再分发依据；
6. 已知兼容性限制和回退路径。

APK 可以内嵌正常运行所需的模型及许可允许再分发的运行时文件。不得把 SDK 头文件、开发工具、签名材料或许可不明确的资产放入 APK 或其他 Release 附件。

## 发布前检查

- 从干净提交构建，确认工作区没有未审计文件混入。
- 对 APK 解包，核对模型、原生库、字体和虚拟形象清单。
- 验证签名、版本号、包名、目标 ABI 和安装升级路径。
- 运行敏感信息与禁止文件扫描。
- 计算附件 SHA-256，并在上传后重新下载校验。
- 许可或再分发范围不明确时停止发布，完成书面确认后再继续。

GitHub 自动生成的源码压缩包不等同于官方 APK。用户应从同一 Release 的附件区下载带 SHA-256 的签名 APK。
